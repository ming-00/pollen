<%= form_for(@forumpost) do |f| %>
    <div class="field">
        <div>
            <%= f.text_area :content, placeholder: "Post a new message in forum now..." %>
        </div>
        <%= f.submit "Create", class: "btn btn-primary" %>
    </div>
<% end %>