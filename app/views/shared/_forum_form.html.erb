<%= form_with(model: @forumpost, local: true) do |f| %>
    <div class="field">
        <%= f.text_area :content, placeholder: "Compose new forumpost..." %>
    </div>
    <%= f.submit "Post", class: "btn btn-primary" %>
<% end %>